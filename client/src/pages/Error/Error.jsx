import Button from '@material-ui/core/Button';

const Error = () => {

    return (
        <div>
        <h1>Error page is a work in progress, but you are on the wrong page so navigate back... </h1>
        <Button variant="outlined" href="/">Click here to go back home</Button>
        </div>
    )
}

export default Error;